import TaskMasterClient from '@/components/TaskMasterClient';

export const dynamic = 'force-dynamic';
export const runtime = 'edge';

export default function Home() {
  return <TaskMasterClient />;
}
